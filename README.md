# Uptime Kuma Compose

This repository holds a docker compose for uptime kuma with traefik.

## Configuration

### Domain Name

By default, this repository uses a hardcoded domain name for the instance of `uptime.zenvps.xyz`

If you wish to use your own domain name, then you'll need to replace instances of the above domain
with your own domain name.
